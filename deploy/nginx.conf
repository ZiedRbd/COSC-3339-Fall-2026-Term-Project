server {
    listen 80;
    server_name campusdesk.duckdns.org 64.227.3.58;

    location /static/ {
        alias /srv/campusdesk/staticfiles/;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/campusdesk/gunicorn.sock;
    }
}
